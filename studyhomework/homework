#include <stdio.h>

int main()
{
	unsigned char flag = 1;
	for (int i = 0; i < 8; i++) {
		for (int j = 7; j >= 0; j--) {
			printf("%d", (flag >> j) & 1);
		}
		flag <<= 1;
		printf("\n");
	}
	for (int i = 0; i < 10; i++) {
		printf("10101010\n");
		printf("01010101\n");
	}
	
	return 0;
}
